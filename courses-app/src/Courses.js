import React from "react";

const Courses  = () => {
    return (
        <tr>
            <td>List of courses</td>
            <td>
                <button onClick={() => "# "}>
                   Button
                </button>
            </td>
        </tr>
    );
};

export default Courses;